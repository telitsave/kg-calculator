{
  "name": "kg-calculator-server",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "eslint": "^9.1.1",
    "express": "^4.19.2",
    "ts-node-dev": "^2.0.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "node .",
    "prestart": "yarn build",
    "server:watch": "ts-node-dev --respawn --transpile-only src/index.ts",
    "lint:fix": "eslint --cache --ext .ts . --fix",
    "lint": "eslint --cache --ext .ts ."
  },
  "devDependencies": {
    "@eslint/js": "^9.1.1",
    "@types/express": "^4.17.21",
    "globals": "^15.0.0",
    "kg-calculator-typings": "workspace:*",
    "prettier": "^3.2.5"
  }
}
