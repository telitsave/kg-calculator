import { FC, memo } from 'react'
import { DragonCalculator } from 'widgets/dragonCalculator'


const DragonCalculatorPage: FC = memo(() => {
  return <DragonCalculator />
})

export default DragonCalculatorPage
