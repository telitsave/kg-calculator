export interface TalentsParameters {
  bow: {
    rank: Record<number, {
      booksCells: number
      crownsCells: number
    }>
  }
  fire: {
    rank: Record<number, {
      booksCells: number
      crownsCells: number
    }>
  }
  ice: {
    rank: Record<number, {
      booksCells: number
      crownsCells: number
    }>
  }
  poison: {
    rank: Record<number, {
      booksCells: number
      crownsCells: number
    }>
  }
}
